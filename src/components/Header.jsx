import React from 'react';

const Header = () => (
  <header>
    <h1>Todo List</h1>
    <p>Add your tasks below. You can mark them as complete, edit, or remove them.</p>
  </header>
);

export default Header;